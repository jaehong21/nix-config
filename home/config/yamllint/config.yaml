extends: default
rules:
  comments:
    min-spaces-from-content: 0
  document-start: disable
  document-end: disable
  empty-lines:
    max-end: 1
  key-duplicates: enable
  line-length:
    level: warning
    max: 140
  truthy: disable
